export const gameSceneSize = {
  width: 0.8 * window.innerWidth + 0.8 * window.innerWidth / 8,
  height: 0.8 * window.innerWidth / 2 + 0.8 * window.innerWidth / 8
};

export const tableSize = {
  width: 0.8 * window.innerWidth + 0.8,
  height: 0.8 * window.innerWidth / 2
}

export const cellSize = {
  width: tableSize.width / 16,
  height: tableSize.height / 8
}
